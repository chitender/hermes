{{/*
  The CRD is included directly in the Helm chart so that `helm install` creates
  the CRD before deploying the operator Deployment that depends on it.

  For production GitOps workflows where CRDs are managed separately
  (e.g. via Argo CD's crd-install hook), set installCRDs=false in values.yaml
  and apply config/crd/bases/ independently.
*/}}
{{- if (index .Values "installCRDs" | default true) }}
{{ .Files.Get "crds/sync.example.com_vaultedcsyncs.yaml" }}
{{- end }}
